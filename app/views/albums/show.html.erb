<ul class="sidebar-list">
  <li>
    <%= link_to "Back", albums_path, class: "btn btn-primary" %>
    <%= link_to "Edit", edit_album_path, class: "btn btn-primary" %>
    <%= link_to "Delete", album_path(@album.id), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-primary" %>
  </li>
</ul>

<div class="panel panel-primary">
  <div class="panel-heading"><%= @album.artist.name %></div>

  <div class="panel-body">

    <!-- <p>Artist: <%= @album.artist.name %></p> -->
    <!-- <p>Album: <%= @album.album %></p>
    <p>Genre: <%= @album.genre %></p> -->
    <!-- <p>Length: <%= @album.album_length %></p> -->
    <%=image_tag @album.image_url %>

  </div>

</div>
